SUWAPPU_API_KEY=suwappu_sk_your_api_key_here

# Only required for managed execution. The wallet is bound to the quote and simulation.
SUWAPPU_WALLET_ADDRESS=0x_your_managed_wallet_address

# start/run-once remain preview-only unless this is exactly 1 AND --execute is passed.
SUWAPPU_ALLOW_MANAGED_EXECUTION=0

# Client-side ceiling per recurring action. This reference accepts USDC only.
SUWAPPU_MAX_DCA_USDC=1000

# Optional durable journal directory (default: ~/.suwappu-dca).
# Keep this app-exclusive. Local write commands enforce one owner with execution.lock.
# SUWAPPU_DCA_STATE_DIR=./state

# Soft journal target. Only preview records are eligible for automatic
# pruning; failed/completed and unresolved execution evidence is retained.
# SUWAPPU_DCA_JOURNAL_LIMIT=5000

# Bound every Suwappu request (100-30000ms). Execute timeouts remain
# outcome-unknown and must be reconciled, never assumed failed.
# SUWAPPU_OPERATION_TIMEOUT_MS=25000

# Metadata-only API timing/outcome events. Never includes keys, wallet/market
# terms, quote/swap IDs, response bodies, or error text.
# SUWAPPU_API_EVENTS=1

# Optional development endpoint override.
# SUWAPPU_API_URL=https://api.suwappu.bot
